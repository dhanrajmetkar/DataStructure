import java.util.*;
public class Merge {
    public static void main(String[] args) {
        int arr[]={5,2,1,4,9};
        arr=merge(arr);

    }
    static int[] merge(int arr[]);
    {
        if(arr.length==0)
        {
            return arr;
        }
        int left[]=merge(Arrays.)
    }
}
